[package]
name = "eq-swift"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
uniffi = "0.31"

[build-dependencies]
uniffi_build = "0.31"
